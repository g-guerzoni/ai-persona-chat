# NPM Configuration
# Use npm as the package manager for this project

# Save exact versions (no ^ or ~)
save-exact=false

# Use package-lock.json
package-lock=true

# Automatically install peer dependencies
legacy-peer-deps=false
